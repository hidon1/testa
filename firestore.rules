rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Public access: the app no longer requires Firebase Authentication.
      allow read, write: if true;
    }
  }
}
