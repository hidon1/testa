rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isAuthorizedAdmin() {
      return request.auth != null
        && request.auth.token.email == 'zm0548407450@gmail.com';
    }

    match /{document=**} {
      allow read, write: if isAuthorizedAdmin();
    }
  }
}
